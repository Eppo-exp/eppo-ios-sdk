name: Swift

on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: make build

      - name: Pull test data
        run: make test-data

      - name: Run tests
        run: make test
